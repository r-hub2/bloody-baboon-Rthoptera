% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/spectrum_plotly.R
\name{spectrum_plotly}
\alias{spectrum_plotly}
\title{Interactive Spectrum Plot using Plotly}
\usage{
spectrum_plotly(
  wave,
  freq.res = 50,
  db.shade = TRUE,
  ovlp = 0,
  fun = "mean",
  wn = "blackman",
  total.bandwidth = FALSE,
  limit.indices = FALSE,
  plot.title = "",
  italic.title = TRUE,
  fmin = 0,
  fmax = NULL,
  add.params = FALSE,
  add.summary = FALSE,
  x.breaks = 10,
  y.position = "left",
  x.position = "bottom",
  show.x.title = TRUE,
  show.y.title = TRUE,
  color.db = "grey",
  color.linear = "black",
  color.carrier = "white",
  color.threshold = "white",
  color.bandwidth = "white",
  show.lines = FALSE,
  linewidth = 1
)
}
\arguments{
\item{wave}{A wave object containing the audio data.}

\item{freq.res}{Numeric, the frequency resolution in Hz per bin. Default is 50.}

\item{db.shade}{Logical, whether to shade the plot based on decibel amplitude. Default is `TRUE`.}

\item{ovlp}{Numeric, the overlap between successive windows in percentage. Default is 0 (no overlap).}

\item{fun}{Character, the summary function to apply to the spectrum, one of "mean", "median", "var", or "sd". Default is "mean".}

\item{wn}{Character, the window type for the FFT, one of "hanning", "bartlett", "blackman", "flattop", "hamming", or "rectangle". Default is "hanning".}

\item{total.bandwidth}{Logical, whether to calculate the total bandwidth, ignoring gaps. Default is `FALSE`.}

\item{limit.indices}{Logical, whether to limit the spectrum indices to the frequency range between `fmin` and `fmax`. Default is `FALSE`.}

\item{plot.title}{Character, the title of the plot. Default is an empty string.}

\item{italic.title}{Logical, whether to italicize the plot title. Default is `FALSE`.}

\item{fmin}{Numeric, minimum frequency (in kHz) to plot. If `NULL`, the entire frequency range is plotted. Default is `NULL`.}

\item{fmax}{Numeric, maximum frequency (in kHz) to plot. If `NULL`, it defaults to the Nyquist frequency.}

\item{add.params}{Logical, whether to add window parameters as a label on the plot. Default is `FALSE`.}

\item{add.summary}{Logical, whether to add summary statistics as a label on the plot. Default is `TRUE`.}

\item{x.breaks}{Numeric, the number of breaks on the x-axis (frequency axis). Default is 6.}

\item{y.position}{Character, the position of the y-axis, either "left" or "right". Default is "left".}

\item{x.position}{Character, the position of the x-axis, either "top" or "bottom". Default is "bottom".}

\item{show.x.title}{Logical, whether to show the x-axis title. Default is `TRUE`.}

\item{show.y.title}{Logical, whether to show the y-axis title. Default is `TRUE`.}

\item{color.db}{Character, color for the dB amplitude plot. Default is "grey30".}

\item{color.linear}{Character, color for the linear amplitude plot. Default is "black".}

\item{color.carrier}{Character, color for the carrier frequency marker. Default is "white".}

\item{color.threshold}{Character, color for the threshold line. Default is "white".}

\item{color.bandwidth}{Character, color for the bandwidth lines. Default is "white".}

\item{show.lines}{Logical, whether to show lines for the carrier frequency and bandwidths. Default is `FALSE`.}

\item{linewidth}{Numeric, the width of the lines in the plot. Default is 1.}
}
\value{
A Plotly object representing the interactive spectrum plot.
}
\description{
This function generates an interactive spectrum plot using Plotly, offering customizable options for windowing, overlap, amplitude shading (dB and linear), and the ability to display summary statistics and parameter annotations. The function supports both dB and linear scales, bandwidth calculations, and frequency markers.
}
\examples{
 \dontrun{
# Example usage:
spectrum_plotly(Coryphoda_albidicollis, db.shade = FALSE, freq.res = 100, show.lines = TRUE)
}
}
